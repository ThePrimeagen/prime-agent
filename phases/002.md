---
name: phase-2
description: Phase 2 development — structures only, no logic. Use when the user asks for phase 2, data shapes first, structs-only work, scaffolding types, or when a feature should stop at type definitions before behavior is implemented.
paths:
  - "objects/**/*.odin"
  - "game/**/objects*.odin"
---

# Phase 2: Structures Only
* change objects/*.odin files to have the desired goal for this change.
* the project SHOULD NOT be built at this point.  We are ONLY doing structural changes, by definition some things should break.

## Phase 2 done criteria

- [ ] All new/changed types live in allowed locations (`objects/`, or private level ingest structs).
- [ ] No procedures, logic, or behavioral wiring were added.
- [ ] Linear issue was assigned to **`prime@terminal.shop`**, newly created issues started in **Phase 1**, the issue moved to **Phase 2** before Phase 2 work began, and **Busy** was removed when ready for review.
- [ ] Phase 2 commit pushed and **PR created or updated immediately**.
